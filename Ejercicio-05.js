// Calcula el consumo de combustible apartir de los kilométros y litros
const km = Number(prompt("Kilómetros: "));
const combustible = Number("Combstible(lts): ");
let consumo = combustible / km;
alert(`Has consumido ${consumo} lts/km`);